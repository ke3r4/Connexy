# Connexy — Per-Tenant Validation Dossier Template

> This template is generated by the compliance dossier generator (`packages/data/src/compliance.ts`) and exported as part of the validation-ready package. A customer validation reviewer should be able to validate the system from this dossier alone.

---

## 1. System Identity

| Field | Value |
|---|---|
| System Name | Connexy |
| Version | 0.1.0 |
| Tenant ID | `{tenant_id}` |
| Data Residency Region | `{eu\|us\|custom}` |
| Generated At | `{ISO timestamp}` |

## 2. Read-Only Assertion (Prime Directive 1)

Connexy NEVER writes to, mutates, or controls any customer source system or shopfloor equipment.

### Evidence
- **Read-only safety test suite:** 63 tests across 12 connectors — all assert `canWrite: false`, `isReadOnly: true`, `readOnlyVerified: true`
- **Structural enforcement:** No write methods exist on the `ConnectorAdapter` interface (`packages/connectors/src/adapter.ts`)
- **Runtime enforcement:** `assertConfigHasReadOnlyAssertion()` throws if `readOnlyAssertion !== true`
- **Registry enforcement:** `ConnectorRegistry.register()` refuses connectors with `canWrite: true`

### Connector Inventory
| Connector Type | Tier | canWrite | readOnlyVerified |
|---|---|---|---|
| metadata-file | file | false | true |
| sap-erp | erp | false | true |
| siemens-opcenter | mes | false | true |
| aveva-pi | historian | false | true |
| ... | ... | false | true |

## 3. Human-in-the-Loop (Prime Directive 2)

Every AI recommendation is a proposal. Nothing auto-applies.

### Metrics
| Metric | Value |
|---|---|
| Total AI proposals | `{count}` |
| Accepted by humans | `{count}` |
| Rejected by humans | `{count}` |
| Modified by humans | `{count}` |
| Auto-applied | **0** (guaranteed by design) |

## 4. Evidence + Confidence (Prime Directive 3)

Every recommendation includes:
- A calibrated confidence score (0.0–1.0)
- At least one evidence item linking to source metadata
- Evidence types: naming, semantic, metadata, sample, lineage, human

## 5. Audit Trail (Prime Directive 4)

### Chain Integrity
- **Total audit entries:** `{count}`
- **Chain valid:** `{true\|false}` (SHA-256 hash-chained, append-only)
- **Storage:** Per-tenant Durable Object (strong consistency, immutable)
- **Verification:** `GET /api/admin/audit/:entityId` reconstructs and verifies the chain

### Sample Entries
```json
{ "id": "...", "action": "mapping.accept", "userId": "...", "hash": "...", "prevHash": "..." }
```

## 6. Data Residency (Prime Directive 5)

| Path | Scope | Residency |
|---|---|---|
| Connector → D1 | Metadata only | EU |
| AI Gateway → Workers AI (GLM-5.2) | Prompt + response | Cloudflare boundary |
| Vectorize | Embeddings | Cloudflare boundary |

**Enforcement point:** Model router `assertResidency()` blocks calls when tenant region ≠ model region (unless `custom`).

## 7. Cost Discipline (Prime Directive 6)

| Tier | Model | Calls | Cost (USD) |
|---|---|---|---|
| small | llama-3.1-8b-fast | `{n}` | `${x}` |
| large | glm-5.2 (cached) | `{n}` | `${x}` |
| medium | llama-3.3-70b-fp8 | `{n}` | `${x}` |
| embedding | bge-large-en-v1.5 | `{n}` | `${x}` |
| **Total** | | `{n}` | **`${x}`** |

**Spend as % of project price:** `{x}%` (target: < 3%)

## 8. ALCOA+ Assessment

| Principle | Compliant | Evidence |
|---|---|---|
| Attributable | YES | Every action recorded with user ID + timestamp |
| Legible | YES | Structured JSON with action, entityType, entityId |
| Contemporaneous | YES | Timestamps recorded within request lifecycle |
| Original | YES | Append-only, SHA-256 hash chain |
| Accurate | YES | Chain head hash matches computed hash |
| Complete | YES | All AI proposals + human decisions captured |
| Consistent | YES | Single source of truth (D1 + Durable Objects) |
| Enduring | YES | Durable Object storage with Cloudflare durability |
| Available | YES | Queryable via API endpoint |

## 9. Segregation of Duties

| Role | Create | Approve | Export | Compliant |
|---|---|---|---|---|
| admin | Y | Y | Y | YES |
| architect | Y | Y | Y | YES |
| engineer | Y | N | N | YES |
| reviewer | N | Y | Y | YES |
| viewer | N | N | N | YES |

## 10. What Connexy Did

- Read metadata from connected source systems (read-only)
- AI-proposed mappings, models, and gap detections with confidence + evidence
- Tracked all model calls with cost + latency telemetry
- Maintained immutable hash-chained audit log
- Enforced data residency at the model-router boundary

## 11. What Connexy Did NOT Do

- Did NOT write to, modify, or control any source system or shopfloor equipment
- Did NOT auto-apply any AI recommendation (all proposals reviewed by humans)
- Did NOT transfer customer metadata outside the configured residency boundary
- Did NOT route bulk work to premium frontier models
- Did NOT use suspended Mythos-class models